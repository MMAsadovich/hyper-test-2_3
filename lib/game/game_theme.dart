import 'package:flutter/material.dart';

class GameTheme {
  static const bg = Color(0xFF0B0F1A);

  // C1
  static const player = Color(0xFF00E5FF);   // cyan
  static const obstacle = Color(0xFFFF2D95); // pink
}
